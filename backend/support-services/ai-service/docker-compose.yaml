services:
  ai-service:
    build:
      context: ../..
      dockerfile: support-services/ai-service/Dockerfile
    container_name: ai-service
    env_file:
      - ./.env
    ports:
      - "8088:8088"
    restart: always
  networks:
      - alpha_network