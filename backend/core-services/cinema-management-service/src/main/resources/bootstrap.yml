spring:
  application:
    name: cinema-management-service
  cloud:
    config:
      uri: http://localhost:8888 # Cấu hình của service này nằm ở config-server chạy ở port 8888
      fail-fast: true # Nếu không kết nối được với config-server ở port 8888 thì service này sẽ dừng
