package com.movieticket.product.repository;

import com.movieticket.product.entity.AgeType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AgeTypeRepository extends JpaRepository<AgeType, String> {
}
