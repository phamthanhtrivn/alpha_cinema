services:
  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"
    command: redis-server --requirepass 123456
    restart: always
    volumes:
      - redis_data:/data
volumes:
    redis_data: