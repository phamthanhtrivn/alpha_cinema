spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
rsa:
  private-key-path: classpath:keys/private_java.pem
  public-key-path: classpath:keys/public.pem
