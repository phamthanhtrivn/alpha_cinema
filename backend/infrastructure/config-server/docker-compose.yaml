services:
  config-server:
    build:
      context: ../..
      dockerfile: infrastructure/config-server/Dockerfile
    container_name: config-server
    env_file:
      - ./.env
    ports:
      - "8888:8888"
    restart: always
    networks:
      - alpha_network
