export interface ArtistResDTO {
    id: string;
    fullName: string;
    bio: string;
    dateOfBirth: string;
    nationality: string;
    avatarUrl: string;
}

export interface ArtistSummary {
    id: string;
    fullName: string;
}
